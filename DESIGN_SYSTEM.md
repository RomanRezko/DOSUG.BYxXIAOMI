# DOSUG.BY — Дизайн-система

Составлена на основе брендбука **«Гайд по оформлению профиля Instagram»** (DOSUG.BY).
Токены живут в двух местах и синхронизированы:

- **CSS-переменные и компоненты** — `src/app/globals.css`
- **Типизированные токены (TS)** — `src/design-system/tokens.ts`

Шрифты подключены в `src/app/layout.tsx` через `next/font/google`.

---

## 1. Логотип

`DOSUG.BY` — «O» с точкой-глазом образует улыбку (смайл). В точке «i» / «O» —
акцент лаймом. Разрешённые фоны: белый, фиолетовый (`#8012FF`),
светло-лиловый (`#DEC0F1`), тёмно-фиолетовый (`#120D31`).

---

## 2. Цветовая палитра

### Основные цвета (брендбук, стр. 3)

| Название | HEX | Роль | Pantone | RGB | CMYK |
|---|---|---|---|---|---|
| **Violet** | `#8012FF` | Основной бренд-цвет, CTA | 2368 | 128, 18, 255 | 68, 78, 0, 0 |
| **Green Yellow** | `#B6FF61` | Акцент, плашки, выделения | 374 | 182, 255, 97 | 30, 0, 84, 0 |
| **Mauve** | `#DEC0F1` | Светлые фоны, плашки | 263 | 222, 192, 241 | 13, 26, 0, 0 |
| **Burnt Sienna** | `#E9724C` | Акцент, «плохо»/предупреждение | 1645 | 233, 114, 76 | 5, 69, 75, 0 |
| **Russian Violet** | `#120D31` | Текст, тёмные фоны | 3581 | 18, 13, 49 | 90, 90, 48, 65 |

### Дополнительные цвета (стр. 4)

Могут использоваться как альтернатива основной палитре — для разнообразия.

| HEX | Название в коде |
|---|---|
| `#FF1053` | `accent-red` |
| `#4392F1` | `accent-blue` |
| `#FCB97D` | `accent-orange` |
| `#50C5B7` | `accent-turquoise` |
| `#629677` | `accent-sage` |
| `#156064` | `accent-teal` |

### Служебные (не из брендбука)

| HEX | Название | Назначение |
|---|---|---|
| `#6A0FD6` | `violet-dark` | hover/pressed для основного фиолетового |
| `#FFFFFF` | `white` | текст на тёмном, фон |
| `#F8F6FC` | `background-alt` | нейтральный фон страниц |
| `#6B6B8D` | `text-muted` | второстепенный текст |

---

## 3. Сочетание и контраст (стр. 5–6)

**Правило:** сочетание цветов всегда должно быть контрастным. Чем тоньше
начертание / мельче текст — тем сильнее нужен контраст.

- Самый надёжный вариант текста: **белый** на тёмном фоне и
  **тёмно-фиолетовый `#120D31`** на светлом.
- Проверенные контрастные пары «фон → текст» вынесены в `contrastPairs`
  (`tokens.ts`). Примеры: фиолетовый→белый, фиолетовый→лайм,
  лайм→тёмно-фиолетовый, тёмно-фиолетовый→лайм.
- Низкоконтрастные сочетания (лайм-на-синем, фиолетовый-на-лайме мелким
  текстом) — только для крупных заголовков, не для body.

---

## 4. Типографика (стр. 7–10)

### Шрифты

| Роль | Шрифт | Начертания |
|---|---|---|
| Заголовки | **Unbounded** | Regular · Medium · Bold |
| Основной текст | **Random Grotesque Standart** | Book · Medium · Semibold · Bold |

> ⚠️ Random Grotesque Standart — коммерческий шрифт (Displaay), нет в Google
> Fonts. В вебе основной текст набирается **Inter** как ближайшая замена
> (гротеск, полная кириллица). Если появится лицензия — подключить
> Random Grotesque через `next/font/local` и заменить `--font-body`.

### Размеры — оригинал Instagram-макета (холст 1080px)

| Уровень | Размер |
|---|---|
| Заголовок 1 | 128 px |
| Заголовок 2 | 96 px |
| Заголовок 3 | 72 px |
| Основной текст 1 | 56 px |
| Основной текст 2 | 48 px |
| Текст в плашках | 45–75 px |

Допустима лёгкая коррекция межбуквенного интервала: **± до 2%**.

### Веб-шкала (адаптивная, те же пропорции)

`display` `clamp(48–96px)` · `h1` `clamp(32–56px)` · `h2` `clamp(24–40px)` ·
`h3` `clamp(20–28px)` · `h4` `clamp(16–22px)`. Body: `18 / 16 / 14 / 12 px`.

---

## 5. Правила вёрстки текста (стр. 11–14)

1. **«Воздух».** Оставлять пространство между блоками текста, между текстом и
   картинками, поля фрейма. Точных правил нет — определять визуально, но воздух
   обязателен всегда.
2. **Ровные колонки.** Длинный текст — в колонки. Длина строк примерно
   одинаковая, без «зигзагов» по краю (перенос слов или ±2% интервал).
3. **Перенос предлогов.** Предлоги из 1–2 букв не оставляют в конце строки —
   переносят на новую вместе со словом.
4. **Размещение текста.** По умолчанию выравнивание по левому краю; по центру —
   только если один заголовок (или с плашками). Текст ставят в самую пустую
   область фото, не перекрывая лицо/главный объект.

---

## 6. Графические элементы (стр. 15–18)

- **Фотографии** — качественные, яркие, стильные; рекомендуется добавлять
  лёгкий **шум/зернистость** (`.noise-overlay` в globals.css).
- **PNG-объекты** (без фона) — на однотонном фоне, одно или несколько.
- **Плашки** (`.tag`, «плашки») — скруглённые пилюли с текстом; комбинируются
  разные цвета фона/текста при соблюдении контраста.
- **Полосы / волны** (squiggle) — фирменные лаймовые и фиолетовые волнистые
  линии как декоративный акцент.

---

## 7. Токены формы

| Группа | Значения |
|---|---|
| **Радиусы** | sm `8` · md `16` · lg `24` · xl `32` · full `9999` |
| **Отступы** | xs `8` · sm `16` · md `24` · lg `40` · xl `64` · 2xl `96` |
| **Тени** | sm/md/lg на базе `rgba(18,13,49,…)` |
| **Переходы** | fast `150ms` · base `250ms` · slow `400ms` |

---

## 8. Компоненты (готовые классы в globals.css)

- **Кнопки:** `.btn` + `.btn--primary / --secondary / --outline / --dark`, размеры `--lg / --sm`
- **Плашки/теги:** `.tag` + `--green / --mauve / --orange / --outline / --small`
- **Карточки:** `.card` + `--bordered / --dark / --gradient / --accent`
- **Инпуты:** `.input`
- **Рейтинг:** `.rating` (лайм-пилюля)
- **Навигация:** `.nav-link` (+ `--active`)
- **Секции:** `.section`, `.section-title`, `.container`
- **Hero:** `.hero` с фирменными радиальными свечениями
- **Градиенты:** `.bg-gradient-primary / -secondary / -dark`, `.text-gradient`
- **Эффекты:** `.noise-overlay`, `.animate-fade-in-up`, `.animate-glow`

---

## 9. Использование

**В CSS / классах:**
```css
background: var(--color-violet);
color: var(--color-text-light);
border-radius: var(--radius-lg);
```

**В TS/TSX:**
```ts
import { colors, fontSize, contrastPairs } from '@/design-system/tokens';

const cta = { background: colors.violet, color: colors.white };
```
